## Actividad 4

Copíe el código dado principalmente (con los faltantes), se ejecutó, pero solo salió una ventana indicando si se se decidia ejectar con error o sin error, se decidió ejecutar con error, montro otra ventana con la `snake` funcionando, pero con errores en el visual.

![alt text](./img/image2.png)

Priero al empezar a ejecutar el código y modificarlo hubo un error ya aparecía la pantalla sin los nodos, luego de modificar el código y agregarle partes faltantes apareciía solo un nodo, segía al mouse pero no funcionaba correctamente:



